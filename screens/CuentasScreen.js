import { StatusBar } from "expo-status-bar";
import { StyleSheet, Text, View, TextInput, TouchableOpacity } from "react-native";

export default function CuentasScreen(){
    return(
        <View>
            <Text>Esta es la pantalla de cuentas</Text>
        </View>
    );
}